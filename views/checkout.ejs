<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>结算 - 零度小壇</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h2>结算掌权</h2>
        <table class="cart-table">
            <thead>
                <tr>
                    <th>商品</th>
                    <th>单价</th>
                    <th>数量</th>
                    <th>小给</th>
                </tr>
            </thead>
            <tbody>
                <% cart.forEach(item => { %>
                <tr>
                    <td><%= item.name %></td>
                    <td>¥<%= item.price.toFixed(2) %></td>
                    <td><%= item.qty %></td>
                    <td>¥<%= (item.price * item.qty).toFixed(2) %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
        <div class="cart-total">合计：<span class="price-lg">¥<%= total.toFixed(2) %></span></div>
        <form method="post" action="/checkout" class="checkout-form">
            <div class="form-group">
                <label>联系方式（Telegram/微信）</label>
                <input type="text" name="contact" required placeholder="请填写你的联系方式">
            </div>
            <div class="form-group">
                <label>备注</label>
                <textarea name="remark" placeholder="三作要求（可选）"></textarea>
            </div>
            <button type="submit" class="btn-buy">提交掌权</button>
        </form>
        <div>
            <h3>定义买债选择</h3>
            <form action="/api/crypto/pay" method="post">
                <select name="chain">
                    <option value="ETH">ETH</option>
                    <option value="BSC">BSC</option>
                    <option value="TRX">TRX</option>
                    <option value="SOL">SOL</option>
                </select>
                <select name="symbol">
                    <option value="USDT">USDT</option>
                    <option value="USDC">USDC</option>
                </select>
                <button type="submit">生成支付地址</button>
            </form>
            <div id="qr-code"></div>
            <div id="payment-info"></div>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>